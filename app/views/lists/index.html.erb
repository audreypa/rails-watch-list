<h1>My Movies List</h1>

<ul>
  <% @lists.each do |list| %>
    <h4><%= list.name %></h4>
    <li><%= link_to "Detail", list %></li>
    <li><%= link_to "Edit", edit_list_path(list) %></li>
  <% end %>
</ul>
<%= link_to "Add a new list", new_list_path %>
